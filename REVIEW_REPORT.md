# 配色更新報告 - 清除舊侘寂風格、統一 buncomo 風格

**日期：** 2026-03-20
**檔案：** `app.py`

## 修改摘要

### 1. 標題 / 重點文字色 `#1a1a2e` → `#1d1d1f`
涉及位置：
- CSS `h1~h5` 全域標題色
- `[data-testid="stMetricValue"]` 指標數值
- 最新開獎號碼標題（inline style）
- 信心環形圖中央數值
- 對獎驗證標題
- 號碼卡片數字（Tab1 精選）
- 工具箱標題 `.toolbox-label`
- 回測結果中各處命中數字
- 五碼全中機率對比卡片文字

### 2. 命中追蹤表格列底色（舊 wabi-sabi 綠土色 → buncomo 青綠色系）
- `rgba(107,124,90,0.3)` → `rgba(78,205,196,0.2)` （5碼全中）
- `rgba(107,124,90,0.2)` → `rgba(78,205,196,0.15)` （命中3碼以上）
- `rgba(107,124,90,0.15)` → `rgba(78,205,196,0.08)` （命中2碼）
- 文字色 `#a3b18f` → `#1d1d1f` / `#333` / `#555`

### 3. 非活動列文字色
- `#6b7280`（待開獎）→ `#888`
- `#9ca3af`（命中0~1碼）→ `#888`

### 4. 命中趨勢柱狀圖顏色
- 命中2碼：`#a08968`（土色）→ `#FF9671`（橘）
- 命中0~1碼：`#374151`（深灰）→ `#c4c4c4`（淺灰）

### 5. 箭頭裝飾色
- `#ddd` → `#ccc`（五碼全中機率對比卡片中的箭頭）

### 6. 未修改（已符合 buncomo 風格）
以下項目在先前版本已經是正確配色，本次確認無需變動：
- 背景 `#f5f5f7`
- 卡片 `rgba(255,255,255,0.72)` + `backdrop-filter: blur(20px)`
- 主色系 `#4ECDC4` / `#FF6B6B` / `#845EC2` / `#FF9671` / `#FFC75F`
- 內文 `color: #333`、次要文字 `#888` / `#999`
- 邊框 `rgba(0,0,0,0.06)`
- `_dark_layout()` 已使用白底 `#ffffff` + 文字 `#444`
- 環形圖軌道色 `#f0f0f0`（淺灰，符合新風格）
- 手機版 `body, p, label { font-size: 1rem !important; }` 已設定

### 7. 手機版字體確認
- `@media (max-width: 768px)` 中 `body, p, label { font-size: 1rem !important; }` 已確保最小 1rem
- 各 RWD 斷點（768px / 480px / 1024px）邏輯完整

## 驗證
- Python 語法檢查通過（`py_compile`）
- 所有資料邏輯與演算法未變動
- 所有功能保持完整
